const { ethers } = require("ethers");
async function main() {
    const provider = new ethers.JsonRpcProvider("https://sepolia.infura.io/v3/"); // Wait, I don't have the ID easily.
    // I'll use the rpcUrl from config
}
